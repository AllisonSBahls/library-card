import "./style.css"

export default function Navbar(){
    return(
        <nav className="navbar">
            <div>
                Logo
            </div>
            <label>Library Cards</label>
        </nav>
    )
}